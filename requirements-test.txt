# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-test.txt

# Bounded ranges, not hard pins: a `pytest==X.Y.Z` pin blocks routine patch
# upgrades and can silently diverge from pyproject's `dev` extra. The floors
# here must satisfy pyproject.toml's constraints — enforced by
# tests/test_requirements_consistency.py.
pytest>=9,<10
pytest-cov>=4.0
