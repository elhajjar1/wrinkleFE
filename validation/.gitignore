# Generated by running the validation drivers. The committed reference
# figures/CSVs are already tracked and stay tracked regardless of these
# patterns; this only keeps NEW scratch outputs out of git status while
# leaving new .py drivers visible (issue #374).
*.png
*.csv
*.json
*.inp
*.vtk
